import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sonitasvpublico.mundoalfabeto',
  appName: 'Mundo Alfabeto',
  webDir: 'dist'
};

export default config;
